<data>
    <rule else="НА ГИБКОСТЬ БОЛЬШОЕ" if="ОЧЕНЬ ГИБКИМ" />
    <rule else="НА ГИБКОСТЬ БОЛЬШОЕ" if="ОЧЕНЬ ГИБКОЙ" />
    <rule else="НА ГИБКОСТЬ СРЕДНЕЕ" if="НЕМНОГО ГИБКОСТИ" />
    <rule else="НА ГИБКОСТЬ ЛЁГКОЕ" if="ГИБКОСТЬ НЕ ВАЖНА" />
    <rule else="НА СИЛУ БОЛЬШОЕ" if="СИЛЬНЕЕ" />
    <rule else="НА СИЛУ СРЕДНЕЕ" if="ПОБОРОТЬ СЛАБОСТЬ" />
    <rule else="НА СИЛУ ЛЁГКОЕ" if="ДОСТАТОЧНО СИЛЬНЫЙ" />
    <rule else="НА СИЛУ ЛЁГКОЕ" if="ДОСТАТОЧНО СИЛЬНАЯ" />
    <rule else="НА ВЫНОСЛИВОСТЬ СРЕДНЕЕ" if="НЕ ЗАПЫХАТЬСЯ" />
    <rule else="НА ВЫНОСЛИВОСТЬ БОЛЬШОЕ" if="МАРАФОН" />
<rule if="ЖОПА" else="ЧЛЕН" /></data>